# MVP 功能差距分析

> 状态：🟢 评审稿 · Owner: dongqiu · 2026-06-07（增量更新 2026-06-13：报告推送 §2.1、主题页 + 实体追踪 §2.2、成本预算控制 §1.2 闭合）
> 范围：对照 `docs/concept/product-definition.md` 「MVP 包含」清单与 `docs/plan/roadmap.md` M4 未闭合附条件，逐项核实代码实现，标定欠缺项。
> 方法：阅读实际代码（非凭文档），每条缺口给 `file:line` 证据。

## 结论先行

核心链路（采集 → 分析 → 校验 → 报告）已端到端成立，符合 MVP 定义目标（`product-definition.md:97`「让系统端到端跑起来，不是功能完整」）。但对照「MVP 包含」清单与 M4 附条件，仍有若干**明确承诺未兑现**或**打折**项。按三档排：硬缺口 / 软缺口 / 体验缺口，外加运行部署侧未闭合项。

---

## MVP 包含清单逐项核对

| 能力 | 承诺（product-definition.md） | 实际 | 判定 |
|---|---|---|---|
| 采集 | RSS / arXiv / 官方 API；定时增量 + 预处理 + 历史回填 | RSS + arXiv 适配器全覆盖；`initial_digest` 冷启动已实现；部分源被网络层/反爬挡 | ✅ 主体达成（源可达性见 §四）|
| 洞察 | 主题聚合 + 趋势识别 + 信号去噪 + 多源交叉 | analyzer 落地：aggregation/trend type、`importance ≥ 3` 去噪、多源；跨日不复报 P1 已补 | ✅ 达成 |
| 报告 | 每日 brief + 主题深挖 + 可溯源校验 + MD/HTML + 基础全文搜索 | brief/deep_dive/initial_digest；可达性+一致性双校验；MD/HTML；FTS5 搜索+筛选+排序 | ✅ 达成（PPT 超额）|
| 账号 | 注册 / 登录 / **数据隔离** | 单管理员（env 硬编码）；无注册；数据模型无 userId | ✅ **已决策推后**：MVP 口径收敛为单用户，注册/多账号隔离推后（见 §一.1）|
| UI | 报告阅读页 + 最小 Brief + 最小设置 | 四页齐（Brief/报告库/管理看板/设置）+ 报告详情 | ✅ 达成 |

---

## 一、硬缺口：MVP 范围里写了、但没做或只做一半

### 1.1 账号体系 —— 只有单管理员，注册与数据隔离缺失 → ✅ 已决策推后（2026-06-07）

- **承诺（原）**：`product-definition.md` 旧版「最小账号体系（注册 / 登录 / 数据隔离）」；安全设计 line 324「配置 / 数据 / 报告按账号严格隔离」。
- **实际**（`src/auth.ts:15-26`）：单管理员，凭据来自环境变量 `ADMIN_EMAIL/ADMIN_PASSWORD`，JWT session；**无注册流程**。
- **根因层级**：reports / topics / sources 等表**没有 userId 维度**——`src/app/page.tsx`、`src/app/settings/page.tsx` 直接取全量数据。"按账号隔离"在数据模型层就不存在，不是 UI 未接。
- **决策（2026-06-07）**：**MVP 口径收敛为单用户**——单管理员登录即满足，自助注册与多账号数据隔离推后。已落到 `product-definition.md` 六处（v0.7）：①MVP 包含 账号行重写；②MVP 暂不包含 新增"自助注册 / 多账号数据隔离"行；③账号与权限节"按账号隔离"标为目标态 + 加 MVP 口径说明；④安全设计「身份与隔离」行隔离要求标为目标态/MVP 推后（保留鉴权/会话/管理员独立鉴权在范围内）；⑤安全设计「传输与存储」行"删除账号时清除关联数据"标为目标态/MVP 推后；⑥安全设计「合规与版权」行"账号注销与数据删除"标为目标态/MVP 推后。**口径不一致已闭合**——不再是缺口，而是显式推后项。

### 1.2 成本配额 / 上限告警 / 限流 —— 有度量，无管控 → ✅ 已闭合（2026-06-13）

- **承诺**：`product-definition.md:288-291`「可配成本上限（月/日/单次）；触顶告警 + 限流（暂停非关键任务）；成本可视化」。
- ~~**实际**：度量侧已做；可配置上限 + 触顶自动限流/暂停采集未落地。~~
- **闭合（✅ 2026-06-13，见 ADR-0003）**：新增 `src/lib/runtime/cost-guard.ts`（`loadBudgetLimits` / `evaluateBudget` / `getBudgetStatus`）+ `repos.sumRunCostSince`（`SUM(json_extract(cost,'$.amount'))`，日/月 UTC 窗）。三档判定（ok/alert/exceeded），env 配置 `COST_LIMIT_DAILY` / `COST_LIMIT_MONTHLY`（USD）/ `COST_ALERT_PCT`（默认 80）。**自动管线**（`runScheduledPipeline`）每 topic 前查预算，`exceeded` 硬熔断跳过剩余 topic + 告警；**手动操作**（深挖 / 追问）放行但提示（用户主动意图保留应急能力，决策见 ADR-0003）。告警复用 `notifyBudget`（多渠道层）、每 cron 进程去重。admin 顶部「成本预算」卡片显示今日 / 本月用量 + 进度条 + 状态徽标。**未配上限 = 不限 = 零回归**。+30 测试（cost-guard 各档 + 脏输入 + 日/月窗、sumRunCostSince、notifyBudget），typecheck / 410 测试 / build 全绿。见 `operations.md §14`。
- **遗留**：单次（per-call）上限未做（架构原 `quota` 表 per-topic/model 随多账号推后）；真实成本账单定稿仍被直连 key 外部阻塞（见 §1.3）。**待真机端到端验证**（配 `COST_LIMIT_DAILY` 触发一轮 cron 看熔断 + 告警）。

### 1.3 真实成本账单未定稿（外部依赖阻塞）🟡

- roadmap M4 附条件①：直连 `sk-ant-` key 不可得 → Sonnet 降本测不了、Anthropic 列表价干净测算做不了，validator 锁在 Opus relay 上是成本大头。
- **判定**：已知、被外部依赖阻塞，非遗漏。

---

## 二、软缺口：MVP 划为"暂不包含"，但 dogfood 已叩门

> `product-definition.md:110-121` 列为"后续迭代"，严格非 MVP 欠缺，但 daily dogfood 体验立刻撞上。

### 2.1 推送（邮件 / IM）—— 失败告警 ✅ 已闭合；报告推送 ✅ 已闭合（2026-06-13）

- **失败告警（DCP-3 ② ✅ 已闭合，2026-06-07）**：多渠道 adapter 落地——`notifyFailure` 按 `ALERT_WEBHOOK` 自动识别渠道并翻译 payload（feishu 默认主推 / ntfy / slack / discord / generic），飞书可选加签；经两轮独立 AI review 加固（never-throw 兜底、host 点边界、ntfy 空 topic 守卫、probe 解析飞书 code）；**真实飞书 webhook 端到端验证、手机收到告警**。见 `src/lib/runtime/alert.ts`、`operations.md` §12。
- **报告推送（✅ 已闭合，2026-06-13）**：复用同一渠道层把新报告主动推给用户、点进 = 报告 deep-link。`alert.ts` 新增 `reportToNotification`（纯函数 → 带 `<PUBLIC_BASE_URL>/reports/<id>` 链接）、`shouldPushReport`（**空 brief 不推**、deep_dive/initial_digest 始终推）、`notifyReport`（接在 `runReportGen` 落库后，非阻塞永不抛）。两个产品决策：①独立 opt-in `REPORT_PUSH=1`（默认关——失败告警是运维信号、报告推送是内容流，混同渠道默认全开会刷屏）；②空 brief（"本期无重要事件"）自动跳过避免每天噪音。+14 测试，typecheck/380 测试/build 全绿。见 `src/lib/runtime/alert.ts`、`src/lib/agents/pipeline.ts`、`operations.md` §12 末「报告推送」。**待真机端到端验证**（配 `REPORT_PUSH=1` + `PUBLIC_BASE_URL` 后触发一次深挖）。

### 2.2 "主题"导航与主题页缺失 → ✅ 已闭合（2026-06-13）

- IA（`product-definition.md:217`）把「主题」列为主导航，主题页应有"报告时间线 / 关键实体 / 重大事件标注"。
- ~~实际导航仅 4 项，无主题页；主题只能在 settings 里 CRUD。~~
- **闭合（✅ 2026-06-13）**：nav 加「主题」入口（`layout.tsx`，按 IA 顺序 Brief/主题/报告库/看板/设置）；新增 `/topics` 列表页（每主题报告条数 + 最新日期，`topicReportStats` 一次 GROUP BY）与 `/topics/[id]` 详情页——三块正对设计要点：**报告时间线**（`queryReportIndex` 加 `topic` 过滤、date desc，importance≥4 标「重大」徽标）、**关键实体**（跨报告聚合 `entity_names` 按频次 Top 15）、**深挖入口**（复用 `DeepDiveButton`）。
- **连带闭合：实体追踪能力（此前从未实现）✅**：核查发现 product-definition 洞察能力「**实体追踪**（组织/人物/项目/产品维度动态聚合）」从未落地——`report-gen.ts` 把 `index.entity_names` 写死 `[]`、`Insight` 类型无 entity 字段、analyzer 不抽取，故主题页「关键实体」会永远空。本轮补齐全链路：`Entity{name,type}` 类型 + analyzer prompt 规则 11 抽取（LLM schema `.default([])`，省略不丢洞察）+ insight 表加 `entities` 列（幂等迁移）+ report-gen 跨**纳入**洞察聚合去重进 `entity_names`。**真机深挖 `t_code_agents` 端到端验证**：新报告 `rep_0a4bc9c3`（31 洞察/79 引用）抽到数十真实实体（OpenAI/Anthropic/Claude Code/Cursor/SWE-Bench…），主题页渲染出 15 个实体标签。"围绕主题持续聚合演化"的护城河（DCP-1 备忘 `roadmap.md:74`）至此有 UI 承载面。见 `src/app/topics/`、`src/lib/agents/analyzer.ts`、`src/lib/agents/report-gen.ts`、`src/lib/types.ts`。

---

## 三、体验缺口：链路通了，手感未到位（dogfood 已记录）

### 3.1 引用交互停在"跳列表"，无 hover 预览 🟢

`product-definition.md:243` 设计「`[n]` 悬浮=预览卡，点击=来源面板」；当前点 `[N]` 跳底部引用列表（A1 观察焦点 `dogfood-log.md:23`）。已把 `[N]` 渲染成可见蓝色序号 + quote 链接（`dogfood-log.md:53-54`），hover 预览未做。

### 3.2 多轮追问（A4）入口缺席 🟢

报告页设计「页内继续追问」（line 230），实际报告页无此入口。这是 dogfood 当前验证"是否真需要"的头号假设（`dogfood-log.md:37`）——先观察再决定，合理。

### 3.3 进度透明 / 异步任务反馈 🟢

设计要求「流水线步进器 采集→分析→报告，不给空转圈」（line 245）。深挖/立即抓取后目前要去 /admin 数 Run 表（`dogfood-log.md:24`），无实时进度。

---

## 四、运行 / 部署侧未闭合项（roadmap M4 自列）

| 项 | 状态 | 出处 |
|---|---|---|
| 真正部署到生产服务器 | 产物就绪 + 冒烟过；**TLS/反代代码已完整**（`ops/aws/deploy.sh` Caddy 自动 HTTPS，配 `APP_DOMAIN` 即生效，卡在用户提供域名+DNS A 记录）；**卷备份 + off-box DR ✅ 已闭合（2026-06-13，真机部署验证）**；CD 待配 SSH secrets | `roadmap.md:40` |
| 被挡数据源 | HN / google_research / thehackernews 网络层不通；bleeping / freebuf 反爬已 `enabled:false` 下架——**需境外 VPS 复测** | `roadmap.md:43-46` |
| 视频字幕 / ASR spike | 未做（低优）| `roadmap.md:42` |
| 带 key 的定时 eval job | 待直连 key | `roadmap.md` M4 ② |

---

## 优先级建议（目标：个人 dogfood 闭环可持续）

1. ~~**失败告警推送**~~ ✅ **已闭合（2026-06-07）**：多渠道 adapter（飞书默认主推）+ 两轮 review + 真实手机端到端验证，闭合 DCP-3 ②。~~**下一步同类：报告推送**~~ ✅ **已闭合（2026-06-13）**：复用渠道层把新报告推给用户（带 deep-link）+ 空 brief 跳过 + 独立 opt-in `REPORT_PUSH`，门禁全绿，待真机验证（见 §2.1）。
2. ~~**成本上限 + 触顶限流**~~ ✅ **已闭合（2026-06-13，ADR-0003）**：env 可配日/月上限 → 自动管线触顶硬熔断 + 多渠道告警 + admin 用量卡片；手动操作放行但提示。闭合 DCP-3 ① 的限流半（账单定稿仍待直连 key，§1.3）。
3. **生产部署（境外 VPS）** 🟡 —— 一次性解锁 3 个网络层被挡源 + 真实环境。**卷备份 + off-box DR ✅ 已闭合并真机验证（2026-06-13）**：
   - **同卷备份**：`ops/backup-db.mjs` 由 cron 容器每日 18:00 UTC 在线备份（SQLite `db.backup()` 一致快照 + `reports/` 正文）→ `/data/backups/<ts>/`，留 14 份（`BACKUP_KEEP` 可调）retention。cron 服务挂 `/data` 卷、Dockerfile 带脚本、`npm run db:backup` 手动入口。**真机验证**：EC2 重建镜像后手动触发产出 `/data/backups/20260613-105627/`（insight.db 6.1MB + 4 报告文件），备份库 > 在线库证明含 WAL 一致快照；supercronic 已加载 18:00 任务。
   - **off-box DR**：S3 桶 `deep-insight-backups-787182418634`（同区 ap-southeast-1、阻断公开+版本控制+AES256+生命周期 90/30 天）；实例角色 `deep-insight-ssm` 加最小内联策略 `s3-dr-backups`；host cron `/etc/cron.d/deep-insight-dr` 每日 18:30 UTC `aws s3 sync /data/backups → s3://桶/ec2/`。**真机验证**：首次同步 5 对象 6.3MB 上 S3、cron active、干跑命令 OK。一次性搭建脚本 `ops/aws/setup-dr.sh`（幂等）+ `operations.md §6.1.1` 文档。
   - **TLS/反代非代码缺口**——`deploy.sh` Caddy 自动 HTTPS 已完整，卡在用户域名+DNS。
4. ~~主题页~~ ✅（2026-06-13，含实体追踪从未实现的连带补齐，真机验证，见 §2.2）/ ~~追问入口~~ ✅（A4 已落地）/ hover 预览 🟢 —— 仅剩 hover 预览，按 dogfood 摩擦频次决定，不急。
5. ~~**账号注册 + 多用户隔离**~~ ✅ **已闭合（2026-06-07）**：`product-definition.md` v0.7 显式收敛为"单用户 MVP，多账号隔离推后"，口径不一致消除。
