# DCP-3 准入体检（M3 Verify → M4 Launch · 门评前）

> 状态：**准入体检（pre-gate）· 2026-05-28**。本文档**不是门评决策**——它据实核对 DCP-3 准出标准的达成度、厘清可达性硬门槛的测量口径、列出真实缺口与解锁路径，供门评（负责人 + 架构师双签）裁定。
> 准出标准原文见 `docs/plan/m3-plan.md` §「DCP-3 准入」+ `eval-criteria.md` 上线门槛 + DCP-2 四附条件。

## 1. DCP-3 准出标准逐条体检

| # | 准出标准 | 现状 | 判定 | 证据 / 缺口 |
|---|---|---|---|---|
| ① | 引用**可达性通过率 100%**（发布门红线） | 发布层 **100%（构造即成立）**；产出层 yield 89% | ✅ 红线达 | 见 §2（核心论点）。`validator.checkReachability` + `report-gen.selectInsights` 白名单剔除 blocked |
| ① | **幻觉率 ≤2%**（人工抽检，红线） | 未实测 | ❌ 未达 | 需 M3 规模数据 + 人评；`uncoveredClaims` 已提供预筛信号，但无人评真值 |
| ① | 校验器三分类准确率 ≥90% | A1 实测 **82.1%** | 🔶 偏保守 | 低于 90% 目标但**安全向**（over-flag，不放行坏引用）；影响 yield 非安全。负例召回 100%（见下） |
| ① | 负例（not_support）召回 ≥95% | A1 实测 **100%** | ✅ 达 | A1 复验：坏引用零漏网（保守但安全），`a1-runs.md` |
| ② | 成本阈值**实测定稿**（真实账单） | Opus-on-relay provisional（brief¥5/deep_dive¥15/initial¥30） | 🔶 未定稿 | 真实账单 / 列表价干净定稿待**直连 key（不可得）**；现估算落阈内（§3） |
| ③ | 回归 + CI 绿 | 回归门 ✅ · typecheck/test(102)/build ✅ | ✅ 达 | `run-a1` 对照 baseline >3pp 阻断；CI 确定性 eval 覆盖。带 key 定时 eval job 留运维 |
| ④ | 稳定性（不超时 / 重试 / 告警） | 120s+分批不超时 ✅ · maxRetries2 ✅ · 告警 ❌ | 🔶 部分 | 超时/分批根因已修；失败告警 channel 待运维基础设施 |
| ⑤ | 试用反馈闭环 ≥1 轮，无 🔴 | dogfood 已启动，2 份 swe 报告 + security 已修可产出 | ❌ 未闭 | 多人独立评审未固化、完整轮未闭（`dogfood-feedback.md` 待回填） |

**小结**：8 项中 **3 达 · 3 部分/保守 · 2 未达**。两个真正未达的是**幻觉率红线（未实测）**与**试用闭环（未闭）**——二者既非结构性已满足、也非纯外部依赖，是门评前必须补的最小项。

## 2. 核心论点：可达性 100% 是「发布层」红线，已构造即成立

`eval-criteria.md`（第 42/49 行）把「引用可达性通过率 100%」定义为**可溯源底线 / 发布门**红线。本系统在**发布层**对它做**确定性强制**，而非概率达标：

1. `validator.checkReachability`（`validator.ts:25`，纯函数无 LLM）：归一空白后判 `quote` 是否**逐字出现**在被引 `ContentItem.body`——不在即 `fail`。
2. `verdictFor`（`validator.ts:38`）：reachability=fail → verdict=`blocked`（短路，一致性不再评）。
3. `report-gen.selectInsights`（`report-gen.ts:17`）：**白名单**——只保留 verdict 为 `pass`/`flagged` 的引用下标，`blocked` 与「未校验」一律剔除（L31）；洞察若无幸存引用则整条排除。
4. 渲染只遍历 `citationIndices`（md L100-104 / html L151-156）。

**结论：进入发布报告的每一条引用，必然 verdict=pass/flagged → 逐字可达。发布层可达性 = 100%，by construction。**

**那「89%」是什么？** 是 analyzer **产出层 yield**——它产出的引用中 89% 逐字可达，另 11%（长/口语化脏内容上的真改写）被上述闸门**正确挡下**（要么该洞察靠另一条好引用幸存、要么整条被排除）。所以：

- **89% < 100% 不是"红线未达"**，是 yield/覆盖效率指标。提升它（streaming 治本 / body 清洗）= 更多洞察幸存 = **覆盖更全**，但不改变"已发布即 100% 可溯源"这一保证。
- 同理校验器三分类 82.1%（<90% 目标）是**保守 over-flag**：安全向（不放坏引用），代价是误杀拉低 yield；不构成红线失守。

> **建议门评确认这一口径**，并据此在 `eval-criteria.md` 标注「可达性 100% = 发布层度量（validator+report-gen 强制）；analyzer 产出 yield 单列为覆盖指标」。本体检暂不改定标文件，留门评裁定。

## 3. 成本现状（provisional，不阻塞主体）

- 阈值已按 Opus-on-relay 现实重标（`eval-criteria.md` 2026-05-27）：brief ≤¥5 / deep_dive ≤¥15 / initial_digest ≤¥30。
- 实测锚点：brief analyze（6 条 arXiv·opus-4-6）≈ ¥0.53；富多源 brief 经 body 上限 **$1.86→$0.99 砍半**（`cost-perf-2026-05-27.md`）→ 均落阈内。
- 干净定稿（真实账单 / Sonnet 降本 / 列表价）待**直连 key（不可得）**——列**附条件**，不阻塞门评（同 DCP-2 处置）。

## 4. 真实剩余缺口 → 三桶解锁路径

| 桶 | 缺口 | 解锁条件 | owner / 里程碑 |
|---|---|---|---|
| **代码（会话内）** | 产出 yield 89→更高（覆盖）；uncoveredClaims vX.Y 误报 | 截断 streaming 治本（deliberate 大改）+ FP 细化 | 工程 · M3 尾或 M4 早 |
| **运营（需人/时间）** | 试用闭环 ≥1 轮（准出⑤）；幻觉率 ≤2% 实测（准出①）；cron 常态收稳定性 | 团队多人 dogfood + 人评抽检 + 持续运行 | 团队 · M3 内 |
| **外部 key（不可得）** | 成本真实账单定稿；Sonnet 降本（准出②） | 取得直连 `sk-ant-` key | 待资源 · 附条件 |

## 5. 建议门评走向（待双签裁定）

**当前不宜直接门评 DCP-3**——可溯源红线虽达，但**幻觉率红线未实测、试用闭环未闭**，缺最小证据。建议：

1. **门评前补两项最小证据**（均不依赖 key、可短期完成）：
   - **幻觉率首轮人评**：在 dogfood 已产出报告上做小样本人工抽检（≥1 主题 × 全部洞察），给出首版幻觉率数（哪怕样本小）+ uncoveredClaims 命中复核；
   - **试用首轮闭环**：团队 ≥2 人独立按 `dogfood-feedback.md` 维度打勾，回填汇总，确认无 🔴。
2. **补齐后按 DCP-2 范式"有条件通过 → M4"**，将以下列**附条件**：成本真实账单定稿（外部 key）、产出 yield 提升（streaming 治本）、失败告警接线（运维）、带 key 定时 eval job。
3. 若门评接受 §2「发布层 100%」口径，则可达性红线视为**已达**（非附条件）。

> 一句话：**红线本质已守住（发布即 100% 可溯源），门差在"还没用真实试用 + 人评把幻觉率和闭环这两项证据补上"**——这是运营动作，不是代码缺口。

## 6. 评审清单（供负责人 + 架构师勾选）

- [ ] 认可 §2「可达性 100% = 发布层度量、已构造即成立；产出 yield 89% 为覆盖指标」口径？
- [ ] 同意"门评前先补 幻觉率首轮人评 + 试用首轮闭环"两项最小证据？
- [ ] 认可成本按 Opus-on-relay provisional + 真实账单定稿列附条件（外部 key 不卡门）？
- [ ] 校验器三分类 82.1%（保守 over-flag、负例召回 100%）作为安全向偏差接受、列 yield 优化项？
- [ ] 失败告警 / 带 key 定时 eval job 列 M4 运维附条件？

## 7. 双签

- 负责人 dongqiu：待评审
- 架构师：待评审
