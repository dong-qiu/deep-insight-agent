# DCP-2 门评材料包（M2 Develop → M3 Verify）

> 状态：**✅ 有条件通过**（DCP-2 · 2026-05-27，进入 M3 Verify）· 决策记录见 `docs/plan/roadmap.md` DCP 日志。
> 本包由 Claude Code 备齐证据 + 达成度核对 + 建议；**决策由负责人 + 架构师作出**。

## 1. M2 交付完成度（对照 roadmap M2 清单）

| 交付项 | 状态 | 证据 / 备注 |
|---|---|---|
| 端到端 1 条数据流跑通 | ✅ | 采集→分析→校验→报告，真模型 + 真持久化（`m2-backend-integration-2026-05-26.md`） |
| 收集 / 分析 / 校验三段 agent | ✅ | + report-gen；4 agent 全落库 |
| 基础 UI / 输出形态 | ✅ | Next.js：Brief / 报告库 / 看板 / 设置 + 鉴权（实测渲染） |
| 数据源接入 MVP 清单 | ✅ | 23 feed（ai-swe 13 / ai-security 10），rss/arxiv 适配器全覆盖 |
| 容器化 + cron + CI | ✅ | 增量7：Docker standalone + supercronic + GitHub Actions + Dependabot（镜像实起、healthcheck 通） |
| M2 评审修复 | ✅ | round1（4×🔴）+ round2（8×🟡）全收口 |
| 非 web 源 spike | 🔶 部分 | 播客 RSS（show notes）已接入；视频字幕 / ASR 转写未做 |
| **报告类型 deep_dive（主题深挖）** | ✅ 最小确定性版 | 2026-05-27 补：按重要性分节（重点 / 其他）+ 详版溯源（来源数 / 多源），确定性渲染（LLM 叙述润色留迭代）；`runScheduledPipeline` 按 `reportType` 触发（周报）；report-gen 测试覆盖。MVP 承诺已兑现 |
| 报告类型 initial_digest（冷启动首版） | ❌ 未实现 → M3 | 冷启动回填路径未建；优先级低，推 M3 |

## 2. DCP-1 跨门硬门槛达成情况（M2 准出的关键）

### 2.1 A1 验证实跑 —— ✅ **有条件通过**（证据充分）
- 校验环：validator 带思考全量跑通（三分类 82.1%），**负例召回 100% → 安全（坏引用不放行）但保守**；20/20 标注经人评校验为真（`a1-label-audit-2026-05-26.md`）→ 82% 是对可信标签的真实准确率。
- 提炼环：22 条洞察人评固化 **非显然 68%（15/22）、捏造 0**，唯 #14 引用偏少（`a1-ai-prereview-2026-05-26.md`）。
- 详见 `a1-runs.md` / `charter.md`。**遗留 → M3**：② 非 arXiv 多源压测；③ 幻觉率随规模收紧至发布门 ≤2%。

### 2.2 成本实测标定 —— 🟡 **改按中转站现实标定（不依赖 key）**
- eval-criteria 阈值**已按 Opus 现实重标（2026-05-27）**：`brief ≤ ¥5` / `deep_dive ≤ ¥15` / `initial_digest ≤ ¥30`（带思考基准，thinking-off 约减半）。原暂定 `¥0.5/3/8` 假设 Sonnet，不符 Opus-on-relay 现实。
- 现状：brief 与 deep_dive 均已实现（report-gen 确定性、本身 $0；LLM 成本在 analyze+validate，二者同量级，差异来自输入条数）。
- **直连 key 目前不可得**（2026-05-27 确认），故不再"等 key"，改走不依赖 key 的标定：
  - ① 按**中转站真实计价**测量（受控样本 + 对中转站账单），见 §2.3；
  - ② `¥0.5/3/8` 暂定阈本假设 Sonnet，而 MVP 现实是 **Opus-on-relay**（中转站只有 Opus）→ **由产品负责人按 Opus 现实重标**三类阈值（产品决策，不卡 key）；
  - ③ **Sonnet 降本 + Anthropic 列表价干净测算 = 将来有直连 key 再做**（后续优化，非门评阻塞）。
- A1 validator 带思考**已经中转站验证闭合（条件①不依赖 key）**，丢 key 不回退 A1。

### 2.3 成本测量（2026-05-27 受控尝试）
- 受控测量遭**中转站反复超时**：富正文多源 analyze 超时 → 改小 arXiv 切片仍超时（中转站当时不稳，非 prompt 过大）→ 未取到全新干净数。
- **可用实测数（早先 demo 运行 · admin 看板）**：brief `analyze`（6 条 arXiv · opus-4-6）= **$0.073 / 5881 tok ≈ ¥0.53**；`validate` 带思考 $0.66（中转站自缓存上报致虚高、不可信）；validate thinking-off 未取到干净值。
- **判断**：brief 估算 ~¥1.5–5（analyze ¥0.53 + validate；带思考偏上限）→ **已落入重标后的 `brief ≤¥5` 阈内**（原 ¥0.5 暂定阈不符 Opus 现实，已重标，见 eval-criteria）。待中转站稳定时按真实计价实测收紧。
- **附带风险**：测量期间中转站反复超时 → **中转站稳定性是生产风险**（已有 45s 超时 + 2 重试兜底；长任务/高峰需更稳接入或更强重试）。

## 3. 工程与质量状态
- 测试 **83 passed**、typecheck 干净、`next build` 绿、Docker 镜像实起 healthcheck 通。
- 多源广度工程化：限流（F1）+ 相关多样选片（F2）+ SSRF 修复（F3）+ arXiv 限速（F5）；干净重采实测安全侧 5 源多样、软工侧 arXiv 15/15。
- 安全契约：SSRF 网关、`<untrusted-source>` 包裹、白名单纳入闸门、脱敏日志、鉴权门——均在位。

## 4. 风险与战略备忘（承接 DCP-1）
- **一致性校验不得退化为只过可达性** —— ✅ 守住：validator 双层校验 + 100% 负例召回。
- 最警惕竞品 Inoreader；护城河②可溯源一致性 / ③主题持续聚合为最稀缺 —— 当前可溯源链路成立。
- 中转站约束（Opus-only / 无 Sonnet / thinking 缓存计价虚高 / 高峰超时）影响成本与稳定性 —— **直连 key 目前不可得**，故成本改按中转站现实标定 + 重标阈值；Sonnet 降本与列表价干净测算待将来有 key。A1 精度侧已通过中转站带思考验证闭合。
- **新增风险：中转站稳定性**（成本测量中反复超时）——长任务/高峰需更强重试或更稳接入；当前 45s 超时 + 2 重试为兜底。

## 5. 遗留 / 待 M3 闭合清单
1. **成本（不依赖 key）**：阈值已按 Opus 现实重标（brief¥5 / deep_dive¥15 / initial_digest¥30，provisional）；M3 内按中转站真实计价**测实费验证 / 收紧**。直连 key 不可得；Sonnet 降本 + 列表价干净测算留将来。
2. A1 遗留②多源压测、③幻觉率 ≤2%。
3. analyzer 大池分批（F4，宽主题全量分析）；analyzer 提示"数值须被本条引用覆盖"（#14 类）。
4. initial_digest 冷启动回填路径；视频字幕 spike。
5. 沙箱被挡源（HN/google/THN/bleeping/freebuf）生产 IP 复测。
（deep_dive 报告生成已于 2026-05-27 补齐，移出遗留。）

## 6. 评审清单（供负责人 + 架构师勾选）
- [ ] MVP 主链路（brief）是否功能完整到可进 Verify？
- [ ] A1（make-or-break）是否达"有条件通过"且遗留条件可控？
- [ ] 成本：认可已按 Opus 现实重标的三类阈值（brief¥5 / deep_dive¥15 / initial_digest¥30，provisional）+ M3 测实费验证 / 收紧？
- [ ] deep_dive 已补最小确定性版（分节 + 详版溯源）——认可其作为 MVP 深挖起点（LLM 叙述润色留迭代）？
- [ ] 中转站稳定性风险（成本测量反复超时）是否接受为 M3 跟踪项？
- [ ] 遗留清单是否均有 owner + 目标里程碑？

## 7. 建议决策（待评审裁定）
**建议：✅ 有条件继续（CONTINUE，进入 M3 Verify）。** deep_dive 已补；直连 key 不可得但成本不再外部阻塞，改按中转站现实标定：
- **成本**：阈值已按 Opus 现实重标（brief¥5 / deep_dive¥15 / initial_digest¥30）；M3 内按中转站真实计价**测实费验证 / 收紧**（不卡 key）；
- A1 条件① 已闭合（中转站带思考）；遗留 ②多源压测、③幻觉率 ≤2% 在 M3 内闭合；
- **中转站稳定性**列 M3 跟踪项（测量期反复超时）；
- Sonnet 降本 + 列表价干净测算、initial_digest、视频字幕 spike：将来有 key / 优先级低，M3 排期。

> 说明：直连 key 目前不可得；但成本可按中转站现实测量 + 阈值重标推进，不构成外部阻塞 → 不建议 HOLD。

## 8. 双签
- 负责人 dongqiu：✅ 有条件通过（2026-05-27）
- 架构师：待会签
