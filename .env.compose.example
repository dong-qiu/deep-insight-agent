# Compose CLI 变量（docker compose 自动读同目录 .env；与容器运行时的 .env.local 是两回事）。
#
# 工程名默认 = 目录 basename，各 worktree 目录不同 → 容器/数据卷天然隔离，
# 普通临时 worktree 通常【不需要】本文件。只有以下两种情况才 cp 本文件为 .env：
#
#   ① 权威/生产实例（拥有真数据）：钉死 COMPOSE_PROJECT_NAME=deep-insight，
#      保证不管从哪个目录跑都复用同一卷 deep-insight_insight-data，不孤立数据。
#   ② 想同时起多套：设不同 APP_PORT 避端口冲突（端口不随目录自动分配）。
#
# 用法：cp .env.compose.example .env

# 工程名：决定容器名前缀与数据卷全名 <COMPOSE_PROJECT_NAME>_insight-data。
# 留空则回落目录 basename（临时 worktree 的默认隔离方式，无需设本项）。
# 仅权威/生产实例需显式钉成 deep-insight：
COMPOSE_PROJECT_NAME=deep-insight

# app 对外端口（容器内仍是 3000）。同时起多套时各 worktree 取不同端口避免冲突。
APP_PORT=3000
